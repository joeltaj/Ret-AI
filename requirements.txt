# DR-Grade Self-XAI desktop app
# Install with:  pip install -r requirements.txt
# NOTE: install a torch build matching your CUDA/CPU from https://pytorch.org
torch
torchvision
PySide6>=6.6
Pillow
numpy
PyYAML
opencv-python-headless
segmentation-models-pytorch>=0.5
google-genai            # Gemini LLM narrator (optional; falls back to template)
